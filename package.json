{
  "name": "tennu-dblogger",
  "version": "3.0.0",
  "description": "A database logging plugin for the tennu IRC framework.",
  "main": "plugin.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Tennu/tennu-dblogger"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push && git push --tags && npm publish"
  },
  "author": "Victorio Berra",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.0.5"
  },
  "peerDependencies": {
    "tennu-dbcore": "^2"
  }
}
